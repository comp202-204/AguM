import 'package:flutter/material.dart';

class CampusMapDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Campus Map Detail'),
      ),
      body: Center(
        child: Text('Campus Map Detail Page'),
      ),
    );
  }
}
